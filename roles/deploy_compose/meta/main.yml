---
galaxy_info:
  role_name: deploy_compose
  author: mach1el
  description: Rsync a service's deploy folder to a host and run docker compose.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [all]
dependencies: []
