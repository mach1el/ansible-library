all:
  children:
    deploy_targets:
      hosts:
        apexvoid:
          ansible_host: 217.216.74.8
          ansible_port: 51022
          # Deploys connect as the deploy user with the staged vault key.
          # init-env overrides remote_user to root (see playbooks/init-env.yml).
          ansible_user: "{{ deploy_user }}"
          ansible_ssh_private_key_file: "{{ deploy_key_path }}"
